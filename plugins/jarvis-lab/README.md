# Jarvis Lab

Lab-only Cursor plugin that ships a **stdio MCP** (`jarvis-lab`) for reproducing the Attila/Newfold MCP allowlist issue on `ym-test-team`.

- Server: `python3 ${CURSOR_PLUGIN_ROOT}/mcp_launcher.py`
- Tool: `lab_ping` → `{"ok": true, "source": "jarvis-lab"}`

Not for production use.
